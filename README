
ClaroBox.xcodeproj V 2.17.4.00018		Project Xcode Version 8.3 (8E162)

DevicePushKey-Info.plist			Plist content Keys "devicePublicKeyDevelopment" & "devicePublicKeyProduction" for Push Notification
GoogleService-Info.plist			Plist for Google Firebase (Statistic & Push Notification)

Note : this plist now are setup for it.twsweb.Nextcloud 

On $(SRCROOT)/Custom/ClaroBox :

Custom.xcassets					Content image, logo etc.
CustomSwift.h					Bridge Objc-Swift code (do not change it)
LaunchScreen.xib				Launch Screen
Main.xib					Main (do not change it)
NCBrand.swift					Customization and Options

iOSClient.entitlements				Setting Group : group.Bundle Identifier and Keychain : Bundle Identifier 
iOSClient.plist					Setting CFBundleDisplayName, CFBundleShortVersionString, CFBundleVersion

Picker.entitlements				Setting Group : group.Bundle Identifier and Keychain : Bundle Identifier 
Picker.plist					Setting CFBundleDisplayName, CFBundleShortVersionString, CFBundleVersion

PickerFileProvider.entitlements			Setting Group : group.Bundle Identifier and Keychain : Bundle Identifier
PickerFileProvider.plist			Setting CFBundleDisplayName, CFBundleShortVersionString, CFBundleVersion, NSExtensionFileProviderDocumentGroup

Share.entitlements				Setting Group : group.Bundle Identifier and Keychain : Bundle Identifier
Share.plist					Setting CFBundleDisplayName, CFBundleShortVersionString, CFBundleVersion

--- Build ---

Open ClaroBox.xcodeproj and build for internal use (emulator and device connect on Mac) for TestFlight or Production change all file :

DevicePushKey-Info.plist
GoogleService-Info.plist
*.entitlements
*.plist
NCBrand.swift



